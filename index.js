// const ball = document.querySelector(".ball")



document.addEventListener('keydown', function(e) {
    const k = e.key 
    if(k === "ArrowUp" || k === "w") {
        console.log(k)
    } 
    if(k === "ArrowDown" || k === "s") {

    } 
    if(k === "ArrowLeft" || k === "a") {

    } 
    if(k === "ArrowRight" || k === "d") {

    } 
});